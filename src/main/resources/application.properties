spring.application.name=demo

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

spring.jpa.show-sql=true

# Flyway explicitly enabled
spring.flyway.enabled=true
spring.flyway.locations=classpath:db/migration
logging.level.org.flywaydb=DEBUG

debug=true

# Kafka Configuration
spring.kafka.bootstrap-servers=localhost:9092
spring.kafka.admin.auto-create=false

# Producer Configuration
spring.kafka.producer.key-serializer=org.apache.kafka.common.serialization.StringSerializer
spring.kafka.producer.value-serializer=org.springframework.kafka.support.serializer.JsonSerializer

# Consumer Configuration
spring.kafka.consumer.group-id=archiver-group
spring.kafka.listener.ack-mode=MANUAL
spring.kafka.consumer.auto-offset-reset=earliest
spring.kafka.consumer.key-deserializer=org.apache.kafka.common.serialization.StringDeserializer
spring.kafka.consumer.value-deserializer=org.springframework.kafka.support.serializer.JsonDeserializer
spring.kafka.consumer.properties.spring.json.trusted.packages=*

# Redis Configuration (CQRS Fast-Read Path)
spring.data.redis.host=localhost
spring.data.redis.port=6379

# For Upstash Prod version

#spring.data.redis.password=UPSTASH_PASSWORD
#spring.data.redis.ssl.enabled=true

#test
#anomaly.threshold.percentage=0.0001
